# Architettura Multi-Agente
agno

# Gestione Variabili d'Ambiente
python-dotenv

# ORM e Database (Per la persistenza SQLite richiesta)
SQLAlchemy

# Migrazioni schema database (versionamento DDL, sostituisce create_all()).
alembic

groq

# Client SDK per i modelli Google Gemini (vision e parsing via agno)
google-genai

requests

# Parsing HTML per lo scraping dei risultati di ricerca ricette online.
beautifulsoup4

pillow

# Lettura codici a barre dalle immagini.
# zxing-cpp: decoder primario, legge molte simbologie (incluso Code 128) e
# gli screenshot renderizzati che cv2 non decodifica. Wheel self-contained.
zxing-cpp
# opencv: fallback + pre-processing immagini.
opencv-contrib-python-headless

bcrypt

# Creazione/decodifica dei token JWT per l'autenticazione stateless (Bearer).
PyJWT

lancedb

sentence-transformers

# Reader documenti per l'ingestion RAG multi-formato (PDF e DOCX).
pypdf
python-docx
# Richiesto da pypdf per aprire PDF cifrati (AES).
cryptography

torchvision

fastapi

uvicorn

python-multipart

# Suite di Evals (LLM-as-a-Judge): report tabellare in console (evals.py).
# groq, agno e python-dotenv sono gia' presenti sopra e vengono riutilizzati.
tabulate

# Test di sicurezza (tests/test_prompt_injection.py): asserzioni automatiche.
# Opzionale: il runner manuale gira anche senza pytest.
pytest